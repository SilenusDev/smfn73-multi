<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>Symfony + FrankenPHP + Podman</title>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        {{ encore_entry_link_tags('app') }}
    </head>
    <body>
        <div class="container mt-5">
            <div class="row justify-content-center">
                <div class="col-md-8">
                    <div class="card">
                        <div class="card-header bg-success text-white">
                            <h1 class="mb-0">🎉 Succès!</h1>
                        </div>
                        <div class="card-body">
                            <h2 class="text-success">{{ message }}</h2>
                            <p class="mt-3">Votre application Symfony fonctionne correctement avec :</p>
                            <ul class="list-group list-group-flush">
                                <li class="list-group-item">✅ FrankenPHP</li>
                                <li class="list-group-item">✅ Podman</li>
                                <li class="list-group-item">✅ Nginx Proxy</li>
                                <li class="list-group-item">✅ MariaDB</li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        {{ encore_entry_script_tags('app') }}
    </body>
</html>
