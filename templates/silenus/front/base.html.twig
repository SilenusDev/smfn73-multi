<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Silenus{% endblock %}</title>
    
    {% block stylesheets %}
        <style>
            * { margin: 0; padding: 0; box-sizing: border-box; }
            body {
                font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
                background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                min-height: 100vh;
                color: #333;
            }
            .container {
                max-width: 1200px;
                margin: 0 auto;
                padding: 20px;
            }
            header {
                background: rgba(255, 255, 255, 0.95);
                padding: 20px;
                border-radius: 10px;
                margin-bottom: 30px;
                box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            }
            header h1 {
                color: #667eea;
                font-size: 2rem;
                margin-bottom: 10px;
            }
            nav {
                display: flex;
                gap: 20px;
                margin-top: 15px;
            }
            nav a {
                color: #667eea;
                text-decoration: none;
                padding: 8px 16px;
                border-radius: 5px;
                transition: all 0.3s;
            }
            nav a:hover {
                background: #667eea;
                color: white;
            }
            main {
                background: white;
                padding: 40px;
                border-radius: 10px;
                box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            }
            .alert {
                padding: 15px;
                margin-bottom: 20px;
                border-radius: 5px;
            }
            .alert-success {
                background: #d4edda;
                color: #155724;
                border: 1px solid #c3e6cb;
            }
            .alert-error {
                background: #f8d7da;
                color: #721c24;
                border: 1px solid #f5c6cb;
            }
            footer {
                text-align: center;
                color: white;
                margin-top: 30px;
                padding: 20px;
            }
        </style>
    {% endblock %}
</head>
<body>
    <div class="container">
        <header>
            <h1>🌙 Silenus</h1>
            <nav>
                <a href="{{ path('slns_home') }}">Accueil</a>
                <a href="{{ path('slns_about') }}">À propos</a>
                {% if app.user %}
                    <a href="{{ path('slns_logout') }}">Déconnexion ({{ app.user.email }})</a>
                {% else %}
                    <a href="{{ path('slns_login') }}">Connexion</a>
                    <a href="{{ path('slns_register') }}">Inscription</a>
                {% endif %}
            </nav>
        </header>

        <main>
            {% for message in app.flashes('success') %}
                <div class="alert alert-success">{{ message }}</div>
            {% endfor %}
            
            {% for message in app.flashes('error') %}
                <div class="alert alert-error">{{ message }}</div>
            {% endfor %}

            {% block body %}{% endblock %}
        </main>

        <footer>
            <p>&copy; {{ "now"|date("Y") }} Silenus - Tous droits réservés</p>
        </footer>
    </div>

    {% block javascripts %}{% endblock %}
</body>
</html>
